Map-oriented code editor built for standalone programming language.

The WIP source code will be initially written in C at the path `source/`.
The WIP prototype source code is being written in Python at the path `proto/`, that's the main reference for now.

- developed @ ancient_labs